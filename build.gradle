plugins {
    id 'eclipse'
    id 'idea'
    id 'net.minecraftforge.gradle' version '[6.0,6.2)'
    id 'org.spongepowered.mixin' version '0.7.+'
    id 'com.vomiter.forge-base' version "0.1.0"
    id 'com.vomiter.dev-runtime' version "0.1.0"
    id 'com.vomiter.langmerge' version "0.1.0"
}

vomiterMod {
    modId = mod_id
    // 需要的話可以關掉某些功能
    // enableMixinExtras = false
    // enableDevJar = false
}

langMerge { modId = mod_id }

version = mod_version
group = mod_group_id
base { archivesName = mod_id }

java.toolchain.languageVersion = JavaLanguageVersion.of(17)

minecraft {
    mappings channel: mapping_channel, version: mapping_version
    copyIdeResources = true
}

apply from: 'gradle/runs.gradle'
apply from: 'gradle/publishing-local.gradle'
apply from: 'gradle/mixin.gradle'

/*
repositories {
    maven {
        url "https://cursemaven.com"
        content { includeGroup "curse.maven" }
    }
    maven { url "https://maven.blamejared.com" }
}
 */

apply from: 'gradle/optional-dep.gradle'

dependencies {
    minecraft "net.minecraftforge:forge:${minecraft_version}-${forge_version}"
    annotationProcessor("org.spongepowered:mixin:${mixin_version}:processor")
    implementation fg.deobf("curse.maven:timeless-and-classics-zero-1028108:7745481")
    runtimeOnly fg.deobf("curse.maven:playeranimator-658587:4587214")
    implementation fg.deobf("curse.maven:neurolib-1484243:7897734")
}

def devRuntimeProject(String path) {
    evaluationDependsOn(path)
    dependencies {
        runtimeOnly files(
                project(path)
                        .tasks.named("devJar")
                        .flatMap { it.archiveFile }
        )
        compileOnly project(path)
    }
    tasks.matching { it.name in ["runClient", "runServer", "runGameTestServer"] }.configureEach {
        dependsOn("${path}:devJar")
    }
}
//devRuntimeProject(":neurolib")

//devRuntime.project(":common", true)

/*
/summon piglin ~ ~ ~ {IsImmuneToZombification:1,HandItems:[{id:crossbow,Count:1},{}]}
/summon piglin ~ ~ ~ {IsImmuneToZombification:1,HandItems:[{id:diamond_axe,Count:1},{}]}
/summon piglin ~ ~ ~ {IsImmuneToZombification:1, NoAI:1b}
/summon hoglin ~ ~ ~ {IsImmuneToZombification:1,NoAI:1b}
/summon husk ~ ~ ~ {HandItems:[{id: "tacz:modern_kinetic_gun", Count: 1b, tag: {HasBulletInBarrel: 1b, GunFireMode: "AUTO", GunId: "tacz:glock_17", GunCurrentAmmoCount: 29}}
,{}]}
/summon pillager ~ ~ ~ {HandItems:[{id: "tacz:modern_kinetic_gun", Count: 1b, tag: {HasBulletInBarrel: 1b, GunFireMode: "AUTO", GunId: "tacz:type_81", GunCurrentAmmoCount: 29}}
,{}]}

 */
